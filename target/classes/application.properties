spring.application.name=TP-DDS-1
# --- Configuraci�n de Base de Datos PostgreSQL ---

# 1. La URL: Apunta a tu servidor (localhost), puerto (5432) y nombre de la DB (hotelPremier)
spring.datasource.url=jdbc:postgresql://localhost:5432/hotelPremier

# 2. El "traductor" (driver) que debe usar
spring.datasource.driverClassName=org.postgresql.Driver

# 3. Usuario y contrase�a de TU PostgreSQL (pgAdmin)
# Reemplaza <tu_usuario> y <tu_contrase�a> con tus credenciales reales
spring.datasource.username=postgres
spring.datasource.password=1234

# 4. Dialecto: Le dice a Hibernate c�mo crear comandos SQL para PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# 5. �LA M�S IMPORTANTE! Crea/actualiza las tablas autom�ticamente
# Esta l�nea la ten�as bien, la dejamos igual.
spring.jpa.hibernate.ddl-auto=update

server.port = 8081